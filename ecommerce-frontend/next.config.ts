import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Example Next.js specific configurations
  reactStrictMode: true,
  // other next.js configurations...
};

export default nextConfig;
